#!/bin/sh

if [ ! -f "$XDG_CONFIG_HOME/chezmoi/key.txt" ]; then
  age --decrypt --output "$XDG_CONFIG_HOME/chezmoi/key.txt" "{{ .chezmoi.sourceDir }}/key.txt.age"
  chmod 600 "$XDG_CONFIG_HOME/chezmoi/key.txt"
fi
