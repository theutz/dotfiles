alias sp = spotify_player

alias m = ~/.local/bin/mise
alias mx = mise exec

alias lg = lazygit
alias gws = git status --short
alias gwS = git status
alias gfm = git pull
def gpp [] { git pull; git push }
alias gwd = git diff --cached

alias nhds = nh darwin switch
alias nhs = nh search
alias nhos = nh os switch
