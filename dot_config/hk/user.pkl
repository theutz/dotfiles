amends "package://github.com/jdx/hk/releases/download/v1.2.0/hk@1.2.0#/Config.pkl"

hooks {
  ["prepare-commit-msg"] {
    steps {
      ["render-commit-msg"] {
        check = let (file = read("./prepare-commit-msg.nu").uri.replaceFirst("file://", ""))
          #"nu \#(file) "{{commit_msg_file}}" "{{source}}""#
      }
    }
  } 
}

