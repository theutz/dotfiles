yabai -m signal --add event=dock_did_restart action="sudo yabai --load-sa"
sudo yabai --load-sa

yabai -m config layout bsp

gap=10
for dir in bottom left right; do
  yabai -m config "$dir"_padding $gap
done
yabai -m config top_padding 42
yabai -m config window_gap $gap
unset gap

yabai -m config window_opacity on
yabai -m config active_window_opacity 1.0
yabai -m config normal_window_opacity 0.9
yabai -m config window_topmost on
yabai -m config window_shadow float

yabai -m config auto_balance off
yabai -m config split_ratio 0.6

yabai -m config mouse_follows_focus on
yabai -m config window_placement first_child

find "${HOME}/.config/yabai" -not -name "yabairc" -type f -perm +111 -exec {} \;
# vim: ft=bash
