#!/usr/bin/env -S nu

{
  style: round
  width: 8.0
  hidpi: on
  # Weird workaround required when autoload doesn't work with shebang
  # https://github.com/nushell/nushell/issues/15437
  active_color: (nu -e 'rose-pine rose | into argb' | complete | get stdout)
  inactive_color: (nu -e 'rose-pine pine | into argb "dd"' | complete | get stdout)
}
| items {|k, v| [$k $v] | str join "="}
| run-external borders ...$in
