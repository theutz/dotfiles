accordion-padding = 32
after-login-command = []
after-startup-command = ['exec-and-forget sketchybar --reload']
automatically-unhide-macos-hidden-apps = true
default-root-container-layout = 'tiles'
default-root-container-orientation = 'auto'
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true
exec-on-workspace-change = [
  '/bin/bash',
  '-c',
  'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE',
]
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
start-at-login = true

[exec.env-vars]
  PATH = '${HOME}/bin:/opt/homebrew/bin:/opt/homebrew/sbin:${PATH}'

[key-mapping]
  preset = 'qwerty'

[gaps]
  inner.horizontal = 16
  inner.vertical   = 16
  outer.bottom     = 16
  outer.left       = 16
  outer.right      = 16
  outer.top        = [{ monitor."SAMSUNG" = 56 }, 16]

[workspace-to-monitor-force-assignment]
  1 = "main"
  9 = ["secondary", "main"]

[[on-window-detected]]
  if.app-id                       = "wezterm-gui"
  if.window-title-regex-substring = "spotify_player"
  run                             = ['layout floating']

[mode.main.binding]
  # spellcheck:ignore
  # All possible keys:
  # - Letters.        a, b, c, ..., z
  # - Numbers.        0, 1, 2, ..., 9
  # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
  # - F-keys.         f1, f2, ..., f20
  # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
  #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
  #                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
  #                   sectionSign (ISO keyboards only, european keyboards only)
  # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
  #                   keypadMinus, keypadMultiply, keypadPlus
  # - Arrows.         left, down, up, right
  # All possible modifiers: cmd, alt, ctrl, shift

  cmd-ctrl-alt-enter = 'fullscreen'
  cmd-ctrl-alt-h = 'focus --boundaries all-monitors-outer-frame --boundaries-action fail left'
  cmd-ctrl-alt-i = [
    'exec-and-forget aerospace focus-back-and-forth || aerospace workspace-back-and-forth',
  ]
  cmd-ctrl-alt-j = 'focus --boundaries all-monitors-outer-frame --boundaries-action fail down'
  cmd-ctrl-alt-k = 'focus --boundaries all-monitors-outer-frame --boundaries-action fail up'
  cmd-ctrl-alt-l = 'focus --boundaries all-monitors-outer-frame --boundaries-action fail right'
  cmd-ctrl-alt-leftSquareBracket = 'focus-monitor --wrap-around next'
  cmd-ctrl-alt-m = 'fullscreen'
  cmd-ctrl-alt-n = 'workspace next'
  cmd-ctrl-alt-o = 'focus-monitor --wrap-around next'
  cmd-ctrl-alt-p = 'workspace prev'
  cmd-ctrl-alt-rightSquareBracket = 'focus-monitor --wrap-around prev'
  cmd-ctrl-alt-slash = 'layout accordion tiles'
  cmd-ctrl-alt-space = 'mode aero'
  f13 = 'mode aero'

[mode.aero.binding]
  1 = 'workspace 1'
  2 = 'workspace 2'
  3 = 'workspace 3'
  4 = 'workspace 4'
  5 = 'workspace 5'
  6 = 'workspace 6'
  7 = 'workspace 7'
  8 = 'workspace 8'
  9 = 'workspace 9'
  a = 'layout accordion tiles'
  alt-1 = [
    'summon-workspace 1',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-2 = [
    'summon-workspace 2',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-3 = [
    'summon-workspace 3',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-4 = [
    'summon-workspace 4',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-5 = [
    'summon-workspace 5',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-6 = [
    'summon-workspace 6',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-7 = [
    'summon-workspace 7',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-8 = [
    'summon-workspace 8',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-9 = [
    'summon-workspace 9',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-h = [
    'join-with left',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-j = [
    'join-with down',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-k = [
    'join-with up',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  alt-l = [
    'join-with right',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  b = 'exec-and-forget aerospace focus-back-and-forth || aerospace workspace-back-and-forth'
  e = 'balance-sizes'
  enter = "fullscreen"
  equal = 'resize smart +50'
  esc = 'mode main'
  f = 'layout floating tiling'
  f13 = 'mode main'
  h = 'focus --boundaries all-monitors-outer-frame --boundaries-action fail left'
  j = 'focus --boundaries all-monitors-outer-frame --boundaries-action fail down'
  k = 'focus --boundaries all-monitors-outer-frame --boundaries-action fail up'
  l = 'focus --boundaries all-monitors-outer-frame --boundaries-action fail right'
  leftSquareBracket = 'focus-monitor --wrap-around prev'
  minus = 'resize smart -50'
  n = 'workspace next'
  o = 'focus-monitor --wrap-around next'
  p = 'workspace prev'
  r = [
    'reload-config',
    'exec-and-forget sketchybar --reload',
    'exec-and-forget noti -t "aerospace" -m "config relaoded!"',
  ]
  rightSquareBracket = 'focus-monitor --wrap-around next'
  shift-1 = [
    'move-node-to-workspace 1',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-2 = [
    'move-node-to-workspace 2',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-3 = [
    'move-node-to-workspace 3',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-4 = [
    'move-node-to-workspace 4',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-5 = [
    'move-node-to-workspace 5',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-6 = [
    'move-node-to-workspace 6',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-7 = [
    'move-node-to-workspace 7',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-8 = [
    'move-node-to-workspace 8',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-9 = [
    'move-node-to-workspace 9',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-h = [
    'move left',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-j = [
    'move down',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-k = [
    'move up',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-l = [
    'move right',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-leftSquareBracket = [
    'move-workspace-to-monitor --wrap-around next',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-n = [
    "move-node-to-workspace next",
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-o = [
    'move-node-to-monitor --focus-follows-window --wrap-around next',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-p = [
    "move-node-to-workspace prev",
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-rightSquareBracket = [
    'move-workspace-to-monitor --wrap-around prev',
    'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  ]
  shift-v = 'layout horizontal vertical'
  tab = 'exec-and-forget aerospace focus-back-and-forth || aerospace workspace-back-and-forth'
  v = 'layout vertical horizontal'
