# Tmux Configuration

{{- range (glob (joinPath .chezmoi.sourceDir "dot_config" "exact_tmux" "*.conf")) }}
{{- $target_dir := (dir $.chezmoi.targetFile) }}
# {{ include . $ | sha256sum }}
source-file {{ joinPath $target_dir . }}
{{- end }}

# vim: ft=tmux.tmpl
