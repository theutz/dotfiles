#!/usr/bin/env zsh

# Setup git hooks
git config core.hooksPath hooks

# Get location of the script currently running
SCRIPT_DIR=$0:a:h
cd "$SCRIPT_DIR"/../layouts

# Run stow on the layouts
for layout in *; do
  stow --target $HOME --verbose $layout;
done;
