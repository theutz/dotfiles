# Setup keychain
eval "$(keychain --eval --quiet --agents 'ssh,gpg' "$KEYCHAIN_KEYS[@]")"

# Setup starship
eval "$(starship init zsh)"

# Setup shared history
eval "$(atuin init zsh)"

# vim: ft=sh
# Local Variables:
# mode: sh
# End:
