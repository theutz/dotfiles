#!/usr/bin/env zsh

DOTFILES_DIR=$HOME/.dotfiles

cd $DOTFILES_DIR

git stash --include-untracked
git pull
brew bundle --global dump --force
git add home/.Brewfile
git commit -m 'Update global brewfile'
git push
git stash pop
