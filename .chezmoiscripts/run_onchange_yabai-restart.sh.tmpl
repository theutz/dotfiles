#!/usr/bin/env nu

# yabairc hash: {{ include "dot_config/exact_yabai/executable_yabairc" | sha256sum }}
try { pueue group add yabai } catch {|e| print -e $e.msg }
pueue kill -g yabai
with-env { SHELL: nu-login } {
    pueue add -g yabai -- yabai
}
pueue start -g yabai
pueue clean -g yabai
